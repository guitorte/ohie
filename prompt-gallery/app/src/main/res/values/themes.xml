<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        A minimal Material 3 theme used only while the Activity starts up.
        The real, dynamic-color-aware theme is applied in Compose
        (see com.promptgallery.ui.theme.PromptGalleryTheme).
    -->
    <style name="Theme.PromptGallery" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
